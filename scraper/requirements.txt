requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2
lxml_html_clean==0.4.3
feedparser==6.0.11
facebook-scraper==0.2.59
instaloader==4.13.1
langdetect==1.0.9

# ML dependencies (torch, transformers) live in requirements-ml.txt so that
# test and lint environments stay lightweight. The scraper needs both files.

schedule==1.2.2
python-dotenv==1.0.1

# Testing
pytest==8.2.2
pytest-mock==3.14.0
pytest-cov==5.0.0
responses==0.25.3
